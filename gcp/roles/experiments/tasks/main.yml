---
- name: Make Grafana accessable
  include_tasks:
    file: grafana.yml

- name: Test Experiment
  include_tasks:
    file: experiment.yml
  loop: "{{ experiments }}"
  loop_control:
    loop_var: experiment
